function animal(obj){
  return 'This ' + obj.color + ' ' + obj.name + ' has ' + obj.legs + ' legs.';  
}

/*

BEST PRACTICE: 

function animal(obj){
  return `This ${obj.color} ${obj.name} has ${obj.legs} legs.`;
}

*/